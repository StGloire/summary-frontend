// api/client.ts

// Remplacez par votre IPv4 actuelle trouvée avec ipconfig
const IP_ADDRESS = "192.168.100.144"; 
const PORT = "5000";

const API_URL = `http://${IP_ADDRESS}:${PORT}`;

export default API_URL;